<div class="well">
    <h2>{{ post.title }}</h2>
    {{ sylius_blogger_cut_text(post.content, 200, '...', true, true) }}

    <br /> <br />
    <code>Napisao <strong>{{ post.author }}</strong> u {{ post.createdAt|date('Y-m-d H:i:s') }}.</code>

    <div class="pull-right">
    <a href="{{ path('sylius_blogger_post_show', {'id': post.id, 'slug': post.slug}) }}" class="btn">čitaj dalje...</a>
    </div>
</div>

{% extends 'SyliusSandboxBundle:Backend:layout.html.twig' %}

{% block header %}
    <h1>Editing tax rate <small>"{{ rate.name }}"</small></h1>
{% endblock %}

{% block content %}
<form action="{{ path('sylius_sandbox_backend_tax_rate_update', {'id': rate.id}) }}" method="post" class="form-horizontal" novalidate>
    {% include 'SyliusSandboxBundle:Backend/TaxRate:_form.html.twig' %}
    <div class="form-actions">
       <input type="submit" value="save changes" class="btn btn-primary btn-large" />
    </div>
</form>
{% endblock %}

{% extends 'knp_menu.html.twig' %}

{% block label %}{{ item.labelAttribute('iconClass') is empty ? '' : '<i class="%s"></i>'|format(item.labelAttribute('iconClass'))|raw }}{{ item.label|trans(item.getExtra('translation_params', {}), item.getExtra('translation_domain', 'SandboxCoreBundle')) }}{% endblock %}
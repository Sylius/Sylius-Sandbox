{% extends 'SandboxInstallerBundle:Setup:layout.html.twig' %}

{% block content %}

<div class="page-header"> 
  <h1>Sylius sandbox setup.</h1> 
</div> 

<p>
<strong>Welcome,</strong> please choose task for the installer.
</p>

<div class="row"> 
    <div class="span8">
        <div class="installer-option-desc">
        Pick this option if you want fresh, clean application install.
        {% if installed %}
        <span class="label important">Sylius-Sandbox is already installed.</span>
        {% endif %}
        </div>
        
        <div class="installer-option-desc">
        This option is used to update the application. <br />
        <strong>This feature is not implemented yet.</strong>
        </div>
        
        <div class="installer-option-desc">
        Rapair application after crash, unsuccessful plugin installation or any other
        problem, please note that this is experimental tool. <br />
        <strong>This feature is not implemented yet.</strong>
        </div>
    </div> 
    <div class="span6">
        <div class="installer-option">
        {% if installed %}
        <a class="btn primary large disabled">install Sylius sandbox &raquo;</a>
        {% else %}
        <a href="{{ path('sylius_installer_setup_start', {'setupAlias': 'sandbox-install'}) }}" class="btn primary large">install Sylius sandbox &raquo;</a>
        {% endif %}
        </div>
        
        <div class="installer-option">
        <a class="btn primary large disabled">update Sylius sandbox &raquo;</a>
        </div>
        
        <div class="installer-option">
        <a class="btn primary large disabled">repair Sylius sandbox &raquo;</a>
        </div>
    </div> 
</div> 

{% endblock %}
